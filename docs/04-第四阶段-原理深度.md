# 第四阶段：原理深度

> 预计学习时间：2-3 天

---

## 任务 4.1：Webpack 整体流程与手写简易版

### 4.1.1 核心概念

#### Webpack 核心流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        Webpack 打包流程                          │
└─────────────────────────────────────────────────────────────────┘

  1. 初始化          2. 编译              3. 输出
  ┌─────────┐      ┌──────────┐        ┌─────────┐
  │ 读取    │ ───▶ │ 解析入口  │ ──────▶ │ 生成    │
  │ 配置    │      │ 文件     │         │ Bundle  │
  └─────────┘      └──────────┘         └─────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ 依赖分析     │ ◀─────┐
                  │ (AST 解析)   │       │
                  └──────────────┘       │
                         │               │
                         ▼               │
                  ┌──────────────┐       │
                  │ 递归处理     │ ──────┘
                  │ 所有依赖     │
                  └──────────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ 代码生成    │
                  │ (模板替换)   │
                  └──────────────┘
```

#### 核心术语详解

| 术语 | 说明 |
|------|------|
| **Compiler** | 全局编译器，管理整个构建过程的生命周期 |
| **Compilation** | 单次编译对象，包含当前构建的所有模块和资源 |
| **Module** | 模块，每个源文件（JS/CSS/图片等） |
| **Chunk** | 代码块，打包过程中的中间产物 |
| **Bundle** | 最终输出的文件 |
| **AST** | 抽象语法树，用于分析代码结构 |

#### 详细流程步骤

```
1. 初始化 (Initialization)
   - 读取配置文件
   - 加载插件
   - 初始化 Compiler

2. 编译 (Compilation)
   - 创建 Compilation 对象
   - 从入口开始
   - 递归解析依赖
   - 构建模块依赖图

3. 模块处理 (Module Processing)
   - 调用 Loader 处理文件
   - 解析 AST
   - 分析 import/require
   - 递归处理依赖

4. 代码生成 (Code Generation)
   - 生成 Chunk
   - 调用 Plugin
   - 生成 Bundle 代码

5. 输出 (Emitting)
   - 写入文件系统
   - 触发完成钩子
```

---

### 4.1.2 手写简易版 Webpack

#### 整体架构

```javascript
const fs = require('fs');
const path = require('path');
const babylon = require('babylon');
const traverse = require('@babel/traverse').default;
const generate = require('@babel/generator').default;

class SimpleWebpack {
  constructor(options) {
    this.options = options;
    this.entry = options.entry;
    this.output = options.output;
    this.modules = {};    // 存储所有模块
    this.chunks = {};    // 存储所有 chunk
  }
  
  // 1. 开始构建
  run() {
    console.log('🚀 Starting build...');
    
    // 解析入口模块
    const entryModule = this.buildModule(this.entry);
    
    // 创建 chunk
    this.chunks[this.output.filename || 'main.js'] = {
      name: this.output.filename || 'main.js',
      entryModule,
      modules: Object.keys(this.modules),
    };
    
    // 输出文件
    this.emitFiles();
    
    console.log('✅ Build complete!');
  }
  
  // 2. 解析模块
  buildModule(modulePath) {
    // 2.1 读取文件内容
    const originalCode = fs.readFileSync(modulePath, 'utf-8');
    
    // 2.2 生成依赖列表
    const dependencies = [];
    const ast = babylon.parse(originalCode, {
      sourceType: 'module',
      plugins: ['jsx', 'typescript'],
    });
    
    // 2.3 遍历 AST 查找依赖
    traverse(ast, {
      ImportDeclaration: ({ node }) => {
        dependencies.push(node.source.value);
      },
      CallExpression: ({ node }) => {
        // 处理 require()
        if (node.callee.name === 'require') {
          dependencies.push(node.arguments[0].value);
        }
      },
    });
    
    // 2.4 转换代码（简化版）
    const transformedCode = originalCode;
    
    // 2.5 存储模块
    const moduleId = './' + path.relative(process.cwd(), modulePath);
    this.modules[moduleId] = transformedCode;
    
    // 2.6 递归处理依赖
    dependencies.forEach(dep => {
      const depPath = this.resolvePath(modulePath, dep);
      this.buildModule(depPath);
    });
    
    return moduleId;
  }
  
  // 3. 解析路径
  resolvePath(modulePath, dep) {
    // 相对路径
    if (dep.startsWith('.')) {
      return path.resolve(path.dirname(modulePath), dep);
    }
    
    // node_modules
    return path.resolve(process.cwd(), 'node_modules', dep);
  }
  
  // 4. 生成 Bundle
  emitFiles() {
    const outputPath = path.join(this.output.path, this.output.filename || 'main.js');
    
    // 生成自执行函数
    const bundleCode = this.generateBundle();
    
    // 确保目录存在
    fs.mkdirSync(this.output.path, { recursive: true });
    
    // 写入文件
    fs.writeFileSync(outputPath, bundleCode);
    
    console.log(`📦 Bundle written to: ${outputPath}`);
  }
  
  // 5. 生成 Bundle 代码
  generateBundle() {
    const modulesCode = Object.keys(this.modules).map(moduleId => {
      return `'${moduleId}': function(module, exports, require) {
        ${this.modules[moduleId]}
      }`;
    }).join(',\n');
    
    return `
(function(modules) {
  // 缓存已加载的模块
  var installedModules = {};
  
  // require 函数实现
  function require(moduleId) {
    // 检查缓存
    if (installedModules[moduleId]) {
      return installedModules[moduleId].exports;
    }
    
    // 创建模块并缓存
    var module = installedModules[moduleId] = {
      i: moduleId,
      l: false,
      exports: {}
    };
    
    // 执行模块代码
    modules[moduleId].call(module.exports, module, module.exports, require);
    
    // 标记为已加载
    module.l = true;
    
    return module.exports;
  }
  
  // 加载入口模块
  return require('${this.entry}');
})({
  ${modulesCode}
});
    `.trim();
  }
}

// 导出
module.exports = SimpleWebpack;
```

#### 使用简易版 Webpack

```javascript
// build.js
const SimpleWebpack = require('./SimpleWebpack');
const path = require('path');

const webpack = new SimpleWebpack({
  entry: './src/index.js',
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
});

webpack.run();
```

---

### 4.1.3 打包产物分析

#### Webpack 5 打包产物结构

**入口文件**：
```javascript
/******/ (function() { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({
/******/ 		"./src/index.js":
/******/ 		(function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {
/******/ 			__webpack_require__.r(__webpack_exports__);
/******/ 			/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/******/ 				"add": function() { return add; },
/******/ 				"multiply": function() { return multiply; }
/******/ 			});
/******/ 			function add(a, b) { return a + b; }
/******/ 			function multiply(a, b) { return a * b; }
/******/ 			console.log('index.js loaded');
/******/ 		}),
/******/
/******/ 		"./src/math.js":
/******/ 		(function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {
/******/ 			__webpack_require__.r(__webpack_exports__);
/******/ 			/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/******/ 				"add": function() { return add; }
/******/ 			});
/******/ 			function add(a, b) { return a + b; }
/******/ 		})
/******/ 	});
/******/ 
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = __webpack_modules__;
/******/ 
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = __webpack_module_cache__;
/******/ 
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if (!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/ 
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if (typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/ 
/******/ 	// start up
/******/ 	return __webpack_require__(__webpack_require__.s = "./src/index.js");
/******/ })()
```

**核心模块解释**：

| 模块 | 说明 |
|------|------|
| `__webpack_modules__` | 所有模块的集合 |
| `__webpack_module_cache__` | 模块缓存 |
| `__webpack_require__` | 核心 require 函数 |
| `__webpack_require__.r` | 标记 ES Module |
| `__webpack_require__.d` | 定义 getter |
| `__webpack_require__.o` | 检查对象属性 |

---

### 4.1.4 实战练习

#### 实战 1：绘制 Webpack 流程图

```
入口文件 → 依赖解析 → AST 分析 → 依赖图 → 代码生成 → Bundle
     │           │           │         │
     └───────────┴───────────┴─────────┘
                 递归处理
```

---

#### 实战 2：完善简易版 Webpack

**目标**：添加更多功能

1. **支持 ES Module 导出**
```javascript
// 在 generateBundle 中添加 __webpack_require__.d
```

2. **支持 CommonJS**
```javascript
// 处理 module.exports
```

3. **支持多个入口**
```javascript
// entry 可以是对象
```

---

#### 实战 3：分析打包产物

```javascript
// src/index.js
import { add } from './math';

console.log(add(1, 2));
export default 'index';
```

```javascript
// src/math.js
export function add(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}
```

**打包后分析**：
- 哪些函数被导出？
- 哪些函数被使用？
- Tree Shaking 是否生效？

---

## 任务 4.2：热更新 HMR 原理

### 4.2.1 核心概念

#### HMR vs Live Reload

| 特性 | Live Reload | Hot Module Replacement |
|------|--------------|------------------------|
| 刷新方式 | 刷新整个页面 | 局部更新 |
| 状态保留 | ❌ 丢失 | ✅ 保留 |
| 开发体验 | 一般 | 优秀 |
| 适用场景 | 任何项目 | 单页应用 |

#### HMR 核心原理

```
┌─────────────────────────────────────────────────────────────┐
│                        HMR 原理                              │
└─────────────────────────────────────────────────────────────┘

  1. 文件变化                          2. 编译
  ┌─────────────┐                   ┌─────────────┐
  │  修改文件   │                   │  Webpack    │
  │  index.js   │ ─────────────────▶│  重新编译   │
  └─────────────┘                   └──────┬──────┘
                                           │
                                           ▼
  4. 替换模块                     3. 推送消息
  ┌─────────────┐                   ┌─────────────┐
  │  浏览器     │ ◀────────────────│  WebSocket  │
  │  局部更新   │                   │  推送 HMR   │
  └─────────────┘                   └─────────────┘
                                           │
                                           ▼
                                    ┌─────────────┐
                                    │  JSON/HMR   │
                                    │  Bundle     │
                                    └─────────────┘
```

#### HMR 涉及的文件

| 文件 | 作用 |
|------|------|
| `manifest.json` | 包含更新的模块列表 |
| `xxx.hmr.js` | 包含更新后的模块代码 |
| `webpack-dev-server/client` | 浏览器端的 HMR 客户端 |

---

### 4.2.2 HMR 工作流程

#### 详细步骤

```
1. 监听文件变化
   - 使用 chokidar 或 fs.watch
   - 监听入口文件和依赖文件

2. 重新编译
   - 使用 webpack 重新编译
   - 生成 HMR 产物

3. 推送消息
   - 通过 WebSocket 推送
   - 包含模块 ID 和类型

4. 浏览器接收
   - devServer client 接收
   - 触发 HMR runtime

5. 模块替换
   - 调用 module.hot.accept
   - 替换模块代码
   - 执行回调函数
```

#### HMR 消息类型

| 类型 | 说明 |
|------|------|
| `built` | 编译完成 |
| `sync` | 同步更新 |
| `content hash` | 内容变化 |
| `keep-alive` | 保持连接 |

---

### 4.2.3 module.hot API

#### 常用 API

```javascript
// 检查是否支持 HMR
if (module.hot) {
  // 接受模块更新
  module.hot.accept('./module', callback);
  
  // 清理工作
  module.hot.dispose(callback);
  
  // 检查状态
  const status = module.hot.status();
}

// status() 返回值
// - idle: 等待
// - check: 检查更新
// - prepare: 准备更新
// - dispose: 清理旧模块
// - apply: 应用更新
// - abort: 中断
// - fail: 失败
```

#### 基本示例

```javascript
// src/index.js
import './styles.css';
import { render } from './render';

render();

// 启用 HMR
if (module.hot) {
  module.hot.accept('./render', () => {
    // 重新执行 render
    render();
  });
}
```

```javascript
// src/render.js
export function render() {
  document.getElementById('app').innerHTML = '<h1>Hello HMR!</h1>';
}
```

---

### 4.2.4 CSS HMR

#### style-loader 内置支持

```javascript
// style-loader 自动处理 CSS HMR
// 无需手动配置

// styles.css 更新时
// style-loader 会自动替换 <style> 标签
```

#### 原理

```
1. CSS 更新
     │
     ▼
2. style-loader 监听
     │
     ▼
3. 创建新 <style> 标签
     │
     ▼
4. 移除旧 <style> 标签
     │
     ▼
5. 页面样式更新（无刷新）
```

---

### 4.2.5 Vue/React HMR

#### Vue HMR

```javascript
// vue-loader 自动处理
// 无需手动配置

// 可以监听特定更新
if (module.hot) {
  module.hot.accept('./store', () => {
    // 替换 Vuex store
    store.replaceState(require('./store').default);
  });
}
```

#### React HMR（React 18）

```javascript
// React 18 自带 Fast Refresh
// 等同于 HMR

// babel.config.js
module.exports = {
  presets: [
    ['@babel/preset-react', { runtime: 'automatic' }],
  ],
};

// 无需手动配置
// 修改组件后自动更新（保留状态）
```

---

### 4.2.6 手动配置 HMR

#### webpack.config.js

```javascript
const webpack = require('webpack');
const path = require('path');

module.exports = {
  mode: 'development',
  entry: {
    main: [
      'webpack-dev-server/client?http://localhost:8080',
      'webpack/hot/dev-server',
      './src/index.js',
    ],
  },
  devServer: {
    hot: true,
    liveReload: false,  // 禁用 Live Reload
  },
  plugins: [
    new webpack.HotModuleReplacementPlugin(),
  ],
};
```

#### 完整示例

```javascript
// src/index.js
import './style.css';

let count = 0;

function render() {
  const app = document.getElementById('app');
  app.innerHTML = `
    <h1>Counter: ${count}</h1>
    <button id="btn">Increment</button>
  `;
  
  document.getElementById('btn').addEventListener('click', () => {
    count++;
    render();
  });
}

render();

// HMR
if (module.hot) {
  module.hot.accept('./style.css', () => {
    console.log('CSS updated');
  });
  
  module.hot.dispose((data) => {
    data.count = count;
  });
  
  module.hot.accept(() => {
    count = module.hot.data?.count || 0;
    render();
  });
}
```

---

### 4.2.7 实战练习

#### 实战 1：体验 HMR

```bash
# 1. 安装依赖
npm install webpack webpack-cli webpack-dev-server --save-dev

# 2. 配置
# webpack.config.js
const path = require('path');

module.exports = {
  entry: './src/index.js',
  output: {
    filename: 'bundle.js',
    path: path.resolve(__dirname, 'dist'),
  },
  devServer: {
    static: './dist',
    hot: true,
    port: 8080,
  },
  mode: 'development',
};

# 3. 启动
npx webpack serve

# 4. 修改代码，观察变化
```

---

#### 实战 2：手动实现 HMR

```javascript
// 简易 HMR 实现
const fs = require('fs');
const path = require('path');

class SimpleHMR {
  constructor() {
    this.modules = {};
    this.cache = {};
  }
  
  // 加载模块
  require(modulePath) {
    if (this.cache[modulePath]) {
      return this.cache[modulePath].exports;
    }
    
    const module = {
      id: modulePath,
      exports: {},
    };
    
    this.cache[modulePath] = module;
    
    const fn = this.modules[modulePath];
    fn(module, module.exports, this.require.bind(this));
    
    return module.exports;
  }
  
  // 监听文件变化
  watch(filePath) {
    fs.watch(filePath, (eventType, filename) => {
      console.log(`File ${filename} changed!`);
      this.reload(filePath);
    });
  }
  
  // 重新加载模块
  reload(modulePath) {
    // 清除缓存
    delete this.cache[modulePath];
    
    // 重新加载
    this.require(modulePath);
    
    // 触发更新回调
    if (this.onUpdate) {
      this.onUpdate();
    }
  }
}

module.exports = SimpleHMR;
```

---

## 本阶段总结

### 学习要点

1. **Webpack 整体流程**
   - 初始化 → 编译 → 代码生成 → 输出
   - 理解 Compiler 和 Compilation
   - 理解模块依赖图构建

2. **手写简易 Webpack**
   - 依赖解析
   - AST 分析
   - 代码生成

3. **HMR 原理**
   - WebSocket 通信
   - 模块替换机制
   - 状态保留

### 面试重点

- ✅ Webpack 打包流程
- ✅ 手写简易版 Webpack
- ✅ HMR 原理

### 下阶段预告

- 第五阶段将学习 **工程实践**
- 脚手架集成
- Webpack 5 迁移
- 常见问题排查

### 相关资源
- [Webpack 原理分析](https://webpack.js.org/concepts/)
- [HMR 文档](https://webpack.js.org/concepts/hot-module-replacement/)
